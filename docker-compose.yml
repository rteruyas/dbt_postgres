services:
  postgres:
    image: postgres:16
    container_name: dbt-postgres
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - dbt-postgres-data:/var/lib/postgresql/data

volumes:
  dbt-postgres-data: